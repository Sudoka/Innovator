
- lua viewer, renderaction apply?
  - if so, lua owns the scene graph...

- lua file format!
  - probably use table of tables for multi value vec3 fields
  - mesh computations in lua?

- debug:
  - use new opengl debug output

- statically link dependencies
- port LEGO model import to Lua!

- nodes
  - text
  - indirect buffer